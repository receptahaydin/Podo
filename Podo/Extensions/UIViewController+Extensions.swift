//
//  UIViewControllerExtensions.swift
//  HoMDCA
//
//  Created by Anil Garip on 22.03.2021.
//

import Foundation
import UIKit

extension UIViewController {
    
    func showToastMessage(_ message: String) {
        /*let messageHeight = message.getLabelHeight(font: UIFont(name: "HelveticaNeue", size: 14.0)!, labelWidth: 270)
        var viewHeight = messageHeight + 20
        viewHeight = viewHeight >= 50 ? viewHeight : 50
        let posX = self.view.bounds.midX - 150
        let posY = self.view.bounds.size.height + 15
        
        let toastMessageView = ToastMessageView(frame: CGRect(x: posX, y: posY, width: 300, height: viewHeight))
        self.view.addSubview(toastMessageView)
        toastMessageView.initializeView(message: message)*/
    }
}
